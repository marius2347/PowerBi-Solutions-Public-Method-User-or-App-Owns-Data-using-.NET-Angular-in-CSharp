import { Routes } from '@angular/router';
import { EmbeddedReportComponent } from './embedded-report/embedded-report.component';

export const routes: Routes = [
  { path: '', component: EmbeddedReportComponent }
];
